server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          - classpath:/config-repo/
          - classpath:/config-repo/messages/

logging:
  level:
    root: INFO
    org.hibernate: INFO